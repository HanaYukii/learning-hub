---
contest: AtCoder Beginner Contest 465
date: 2026-07-04
tags: [數位dp, 容斥, 樹, ODT, 數論]
url: https://atcoder.jp/contests/abc465
editorial: https://atcoder.jp/contests/abc465/editorial
source: editorial
verified: false
aside: false
reviewed: 2026-07-04
review_interval: 14
---

# AtCoder Beginner Contest 465 — 學習重點

> 懶人包:每題「題意 + 核心作法」。難度 `~` 為 CF-equivalent。
>
> A–B 熱身略過(比例判斷 `3A>2B`、逐時段累加費用)。從 C 開始列。
>
> 版面:題意含小例、作法含關鍵片段。

### [C. Reverse Permutation](https://atcoder.jp/contests/abc465/tasks/abc465_c)
`模擬` `~800`

::: info 題意
給定整數 N(2≤N≤5×10⁵)與長度 N、由 `o`/`x` 組成的字串 S;另有整數序列 A,初始 A=(1,2,…,N)。

依 k=1,2,…,N 的順序操作:若 S_k=`o` 就反轉 A 的前 k 項(A←(A_k,…,A_1,A_{k+1},…,A_N)),若 S_k=`x` 則不動。

輸出:所有操作結束後的序列 A(空白分隔)。

例:`N=3, S=oxo` → k=1 反轉前 1 項(不變)、k=2 不動、k=3 反轉整串 → 輸出 `3 2 1`。
:::

::: tip 作法
**「反轉前 k 項」等價於「把 k 接到已處理序列尾端後反轉整串」**(前 k 項恰為已處理元素);不真反轉、只記反轉次數奇偶——偶 append 尾、奇 prepend 頭(雙陣列/deque),最後總次數為奇再整體反轉一次。O(N)。

```cpp
// sketch:不真反轉 —— deque + 反轉次數奇偶
deque<int> d; int flip = 0;              // flip = 反轉次數 mod 2
for (int k = 1; k <= n; ++k) {
    if (flip) d.push_front(k);           // 奇:邏輯尾端在 front
    else      d.push_back(k);            // 偶:邏輯尾端在 back
    if (s[k-1] == 'o') flip ^= 1;
}
if (flip) reverse(d.begin(), d.end());   // 總次數為奇 → 整體再反轉
```
:::

### [D. X to Y](https://atcoder.jp/contests/abc465/tasks/abc465_d)
`樹` `數學` `~1200`

::: info 題意
給定整數 X,Y(0≤X,Y≤10¹⁸)與 K(2≤K≤10¹⁸)。

變數 x 初始為 X,每次操作可選任一整數 y 滿足 ⌊x/K⌋=y 或 ⌊y/K⌋=x,並把 x 換成 y。

輸出:使 x 變為 Y 的最少操作次數;共 T(1≤T≤2×10⁵)筆測資,各一行作答。

例:`K=2, X=5, Y=3` → 5→2→1→3(刪、刪、補;⌊5/2⌋=2、⌊2/2⌋=1、⌊3/2⌋=1)共 3 步,1–2 步窮舉不可達,即為最少。
:::

::: tip 作法
操作即 K 進位「刪末位 / 補末位」——**每個整數是 K 元 trie 上的節點,答案 = 兩點樹距離(LCA 型)**:(len_X−p)+(len_Y−p),p 為 K 進位公共前綴長;實作直接 while(x≠y) 把較大者 /=K 計數即可。

<svg viewBox="0 0 360 250" width="360" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="K=2 的 trie:X=5 與 Y=3 的樹距離為 3,LCA 為 1">
  <g stroke="currentColor" stroke-width="1.5" opacity="0.3">
    <line x1="180" y1="30" x2="180" y2="80"/>
    <line x1="120" y1="130" x2="90" y2="180"/>
    <line x1="240" y1="130" x2="210" y2="180"/>
    <line x1="240" y1="130" x2="270" y2="180"/>
  </g>
  <g stroke="var(--vp-c-brand-1)" stroke-width="2" stroke-dasharray="4 4">
    <line x1="150" y1="180" x2="120" y2="130"/>
    <line x1="120" y1="130" x2="180" y2="80"/>
    <line x1="180" y1="80" x2="240" y2="130"/>
  </g>
  <circle cx="180" cy="30" r="12" fill="var(--vp-c-default-soft)"/>
  <circle cx="180" cy="80" r="12" fill="var(--vp-c-default-soft)"/>
  <circle cx="180" cy="80" r="17" fill="none" stroke="var(--vp-c-brand-1)" stroke-dasharray="3 3"/>
  <circle cx="120" cy="130" r="12" fill="var(--vp-c-default-soft)"/>
  <circle cx="240" cy="130" r="12" fill="var(--vp-c-default-soft)" stroke="var(--vp-c-brand-1)" stroke-width="2.5"/>
  <circle cx="90" cy="180" r="12" fill="var(--vp-c-default-soft)"/>
  <circle cx="150" cy="180" r="12" fill="var(--vp-c-default-soft)" stroke="var(--vp-c-brand-1)" stroke-width="2.5"/>
  <circle cx="210" cy="180" r="12" fill="var(--vp-c-default-soft)"/>
  <circle cx="270" cy="180" r="12" fill="var(--vp-c-default-soft)"/>
  <text x="180" y="34" text-anchor="middle" font-size="12" fill="currentColor">0</text>
  <text x="180" y="84" text-anchor="middle" font-size="12" fill="currentColor">1</text>
  <text x="120" y="134" text-anchor="middle" font-size="12" fill="currentColor">2</text>
  <text x="240" y="134" text-anchor="middle" font-size="12" fill="currentColor">3</text>
  <text x="90" y="184" text-anchor="middle" font-size="12" fill="currentColor">4</text>
  <text x="150" y="184" text-anchor="middle" font-size="12" fill="currentColor">5</text>
  <text x="210" y="184" text-anchor="middle" font-size="12" fill="currentColor">6</text>
  <text x="270" y="184" text-anchor="middle" font-size="12" fill="currentColor">7</text>
  <text x="203" y="72" font-size="12" fill="currentColor">LCA</text>
  <text x="150" y="207" text-anchor="middle" font-size="12" fill="currentColor">X = 5 (101)</text>
  <text x="262" y="126" font-size="12" fill="currentColor">Y = 3 (11)</text>
  <text x="180" y="238" text-anchor="middle" font-size="12" fill="currentColor">公共前綴長 p = 1 → 距離 (3−1)+(2−1) = 3</text>
</svg>
:::

### [E. Digit Circus](https://atcoder.jp/contests/abc465/tasks/abc465_e)
`數位dp` `狀壓` `~1800`

::: info 題意
給定十進位整數 N(1≤N<10⁵⁰⁰,以字串讀入)。

三個條件:(1) x 是 3 的倍數;(2) x 的十進位表示含數字 3;(3) x 的十進位表示恰使用 3 種相異數字(表示皆不含前導零)。

輸出:1≤x≤N 中「恰滿足其中一個條件」的 x 的個數,mod 998244353。

例:`N=13` → 6、9、12 只滿足「3 的倍數」,13 只滿足「含數字 3」→ 答案 4;3 因同時滿足 (1)(2) 兩條件,不計。
:::

::: tip 作法
數位 DP,狀態 = 已用數字集合(bitmask 2¹⁰)× mod 3 餘數 × 是否貼緊 N;末端對每個 (mask, m) 檢查三條件恰一成立。

**leading zero 不可算成用過數字 0** 是關鍵坑。O(2¹⁰·10·|N|),|N| 為位數(≤500)。

```cpp
// sketch:數位 DP 轉移 —— 狀態 (mask, m, tight)
for (int i = 0; i < len; ++i)
    for (auto& [st, cnt] : dp) {              // st = (mask, m, tight)
        auto [mask, m, tight] = st;
        int hi = tight ? s[i] - '0' : 9;
        for (int d = 0; d <= hi; ++d) {
            bool lead = (mask == 0 && d == 0);        // 仍在前導零
            int nmask = lead ? 0 : mask | (1 << d);   // 前導零不算用過 0
            ndp[{nmask, (m + d) % 3, tight && d == hi}] += cnt;
        }
    }
// 末端:A=(m==0)、B=mask>>3&1、C=popcount(mask)==3
// A+B+C==1 才計入;mask==0(即 x=0)不計
```
:::

### [F. Sjeltzer?](https://atcoder.jp/contests/abc465/tasks/abc465_f)
`容斥` `差分` `~2200`

::: info 題意
給定 N(1≤N≤3×10⁵)瓶飲料,第 i 瓶有 6 位數字字串 ID S_i(兩兩相異)與整數大小 V_i(1≤V_i≤10⁹)。

接著 Q(1≤Q≤3×10⁵)筆詢問,每筆給兩個 6 位數字字串 x,y,求所有 ID s 滿足「對每一位 k=1..6 都有 x_k≤s_k≤y_k」(逐位比較的 6 維盒,不是數值區間)之飲料的 V 總和。

輸出:每筆詢問一行答案。

例:三瓶 ID/V 為 `111111`/5、`121212`/7、`120000`/100,詢問 `x=111111`、`y=121212` → 前兩瓶每一位都落在 [x_k,y_k] 內,計入;`120000` 數值上介於其間,但第 3 位 0∉[1,1],排除 → 答案 5+7=12。
:::

::: tip 作法
把 ID 視為 10⁶ 格點,**先逐維由小到大累加做 6 維前綴和(zeta 變換),查詢再對 6 個下界做 2⁶ 容斥**把盒子拆成前綴和之差。

前處理 O(6·10⁶),每詢問 O(2⁶·6);某位 x_k>y_k 直接答 0。

```cpp
// sketch:6 維前綴和(zeta)+ 查詢 2^6 容斥
for (int d = 0, p = 1; d < 6; ++d, p *= 10)      // 逐維、由小到大累加
    for (int v = 0; v < 1000000; ++v)
        if (v / p % 10) f[v] += f[v - p];        // f 變成 6 維前綴和
long long ans = 0;
for (int m = 0; m < 64; ++m) {                   // 對 6 個下界容斥
    int id = 0, p = 1; bool out = false;
    for (int k = 5; k >= 0; --k, p *= 10) {      // k = 字串位(低位在後)
        int dig = (m >> k & 1) ? x[k] - '1' : y[k] - '0';  // 下界側取 x_k−1
        if (dig < 0) { out = true; break; }
        id += dig * p;
    }
    if (!out) ans += (__builtin_popcount(m) & 1 ? -1 : 1) * f[id];
}
```
:::

### [G. Sum of Mex of Mod of Linear](https://atcoder.jp/contests/abc465/tasks/abc465_g)
`ODT` `數論` `~3500`

::: info 題意
給定整數 N,M,C,K(1≤N≤2×10⁵、0≤C<M≤10⁹、1≤K≤10⁹)與長度 N 的序列 A(0≤A_i<M)。

接著 Q(1≤Q≤2×10⁵)筆詢問,每筆給 (i,X),先把 A_i 永久改成 X(0≤X<M),再求 Σ_{k=0}^{K−1} mex{(Ck+A_j) mod M | 1≤j≤N},其中 mex 為集合未含的最小非負整數。

輸出:每筆詢問一行答案。

例:`M=4, C=1, K=2, A=(0,1)` → k=0:mex{0,1}=2;k=1:集合為 {1,2},mex=0(缺 0)→ 答案 2+0=2。
:::

::: tip 作法
**固定 k 時 mex = 自 x=(−Ck) mod M 起連續屬於 A 的 run 長 dₓ**,故答案 = Σ cₓ·dₓ(cₓ = 對應到 x 的 k 個數,查詢間不變)。

<svg viewBox="0 0 440 246" width="440" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="圓環上的 run:M=8,起點 x=1 起連續屬於 A 的 run 長 3,即該 k 的 mex">
  <circle cx="220" cy="118" r="62" fill="var(--vp-c-default-soft)"/>
  <path d="M 276.6 61.4 A 80 80 0 0 1 276.6 174.6" fill="none" stroke="var(--vp-c-brand-1)" stroke-width="2" stroke-dasharray="4 4"/>
  <circle cx="264" cy="74" r="8" fill="var(--vp-c-brand-1)"/>
  <circle cx="282" cy="118" r="8" fill="var(--vp-c-brand-1)"/>
  <circle cx="264" cy="162" r="8" fill="var(--vp-c-brand-1)"/>
  <circle cx="158" cy="118" r="8" fill="var(--vp-c-brand-1)"/>
  <circle cx="220" cy="56" r="8" fill="currentColor" opacity="0.25"/>
  <circle cx="220" cy="180" r="8" fill="currentColor" opacity="0.25"/>
  <circle cx="176" cy="162" r="8" fill="currentColor" opacity="0.25"/>
  <circle cx="176" cy="74" r="8" fill="currentColor" opacity="0.25"/>
  <circle cx="264" cy="74" r="13" fill="none" stroke="var(--vp-c-brand-1)" stroke-dasharray="3 3"/>
  <text x="220" y="84" text-anchor="middle" font-size="12" fill="currentColor">0</text>
  <text x="250" y="96" text-anchor="middle" font-size="12" fill="currentColor">1</text>
  <text x="262" y="122" text-anchor="middle" font-size="12" fill="currentColor">2</text>
  <text x="250" y="156" text-anchor="middle" font-size="12" fill="currentColor">3</text>
  <text x="220" y="168" text-anchor="middle" font-size="12" fill="currentColor">4</text>
  <text x="190" y="156" text-anchor="middle" font-size="12" fill="currentColor">5</text>
  <text x="178" y="122" text-anchor="middle" font-size="12" fill="currentColor">6</text>
  <text x="190" y="96" text-anchor="middle" font-size="12" fill="currentColor">7</text>
  <text x="312" y="122" font-size="12" fill="currentColor">run 長 dₓ = 3</text>
  <text x="220" y="216" text-anchor="middle" font-size="12" fill="currentColor">M = 8:實心 = 值屬於 A,空心 = 不屬於</text>
  <text x="220" y="236" text-anchor="middle" font-size="12" fill="currentColor">起點 x = 1(虛線圈)→ run = 1,2,3 → 該 k 的 mex = 3</text>
</svg>

**用 ODT 把 A 當圓環上的極大區間管理**,每次修改只動 O(1) 個區間,差分寫成 c 的區間和,用 **floor sum / 一般化 floor sum** 快速求;A 為全集或恰缺一值要特判。
:::
