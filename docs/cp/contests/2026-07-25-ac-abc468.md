---
contest: AtCoder Beginner Contest 468
date: 2026-07-25
tags: [字串, 組合計數, dp, 貪心, BIT]
url: https://atcoder.jp/contests/abc468
editorial: https://atcoder.jp/contests/abc468/editorial
source: self
verified: true
aside: false
reviewed: 2026-07-26
review_interval: 14
---

# AtCoder Beginner Contest 468 — 學習重點

> 親自參賽，以下記錄當時的解法；程式碼皆通過樣例。難度 `AtC~` 為 kenkoooo difficulty。
> A–C 熱身略過，收 D–G。

### [D. Good Substrings](https://atcoder.jp/contests/abc468/tasks/abc468_d)
`字串` `AtC~700`

::: info 題意
一個小寫字串稱為 good ⟺ **最多改寫一個字元就能變成回文**(回文本身也算 good)。例如 `a`、`iwai`、`abcdcza` 是 good,`abcd`、`atcoder` 不是。

給定長度 $\le 10^4$ 的小寫字串 $S$,**求**它有幾個非空子字串是 good(不同位置取出的子字串即使內容相同也分開計)。

例:$S=$ `abcd` 共 10 個子字串,只有 `abcd` 本身不是 good(首尾 `a`≠`d`、中間 `b`≠`c`,兩對錯位要改兩個字);其餘 9 個都能改 $\le1$ 個字變回文 → 答案 9。
:::

::: tip 作法
把首尾字元依序配對。每一組不相等的配對至少要改一個字元，因此「最多改一個字元能變成回文」就等價於**錯位對不超過一組**。

固定一個回文中心往兩側擴張，同時維護錯位數 `dif`。半徑變大時，`dif` 只會增加；一旦超過 1，後面的半徑也不可能合法，可以直接停止。奇數長與偶數長各掃一次所有中心即可。

每個中心最多向外走 $O(n)$ 步，總複雜度 $O(n^2)$；在 $n\le10^4$ 的限制下足夠。

```cpp
// 奇中心:l = r = i 起步(偶中心同理,l = i、r = i+1)
for (int i = 0; i < n; i++) {
    int l = i, r = i, dif = 0;
    while (l >= 0 && r < n) {
        if (l != r && s[l] != s[r]) dif++;
        if (dif > 1) break;          // dif 單調不減 → 直接收工
        ans++;                        // 這個半徑的子字串是 good
        l--; r++;
    }
}
```
:::

### [E. Sum of Means](https://atcoder.jp/contests/abc468/tasks/abc468_e)
`組合計數` `貢獻` `AtC~1050`

::: info 題意
給定長度 $N \le 5\times10^5$ 的整數序列 $A$($0 \le A_i < 998244353$)。令 $f(l,r)$ 為 $A_l,\dots,A_r$ 的**算術平均**。

**求** $\displaystyle\sum_{1\le l\le r\le N} f(l,r)$ 對 $998244353$ 取模(有理數取模)。

例:$N=2$、$A=(1,3)$ → 三個區間的平均分別是 $1$、$\frac{1+3}{2}=2$、$3$,總和 $6$。
:::

::: tip 作法
不要枚舉區間，改算每個 $A_i$ 對答案的貢獻。它會出現在所有 $l\le i\le r$ 的區間中，每次貢獻 $\frac{A_i}{r-l+1}$，因此

$$\text{答案}=\sum_i A_i \cdot C_i,\qquad C_i=\sum_{l\le i}\sum_{r\ge i}\frac{1}{r-l+1}.$$

設一段長度為 $m$ 的序列中，所有區間的權重總和為

$$G(m)=\sum_{k=1}^{m}\frac{m-k+1}{k}
      =\sum_{j=1}^{m}H_j,\qquad H_j=\sum_{k=1}^{j}\frac1k.$$

要求包含位置 $i$ 的區間，只要從全部區間扣掉完全落在 $i$ 左右兩側的區間：

$$C_i = G(N) - G(N-i) - G(i-1).$$

先算模逆元，再依序掃出 $H$ 與 $G$；每個 $C_i$ 就能 $O(1)$ 取得，整體 $O(N)$。

```cpp
// inv[]:線性求逆元;pre1 = H(調和數前綴)、pre2 = G(H 的前綴)
for (int k = 1; k <= n; k++) pre1[k] = (pre1[k-1] + inv[k]) % mod;
for (int m = 1; m <= n; m++) pre2[m] = (pre2[m-1] + pre1[m]) % mod;
for (int i = 1; i <= n; i++) {
    ll C = ((pre2[n] - pre2[n-i] - pre2[i-1]) % mod + 2*mod) % mod;   // 容斥
    ans = (ans + A[i] % mod * C) % mod;
}
```
:::

### [F. Two Increasing Chains](https://atcoder.jp/contests/abc468/tasks/abc468_f)
`貪心` `dp` `BIT` `AtC~1700`

::: info 題意
給定 $(1,\dots,N)$ 的排列 $P$($N \le 5\times10^5$)與三個變數 $x=y=c=0$。

對 $k=1,\dots,N$ **依序**擇一執行:**操作 1** — 若 $x<P_k$ 則 $c$ 加 1,然後 $x \leftarrow \max(x,P_k)$;**操作 2** — 對 $y$ 做同樣的事。

**求** $c$ 的最大可能值。

例:$P=(2,1,3)$ → 把 $2$ 給 $x$、$1$ 給 $y$、$3$ 給任一邊,三步都讓 $c$ 加 1 → 答案 3(暴力枚舉全部 $2^3$ 種分配確認)。
:::

::: tip 作法
把每個 $P_k$ 分給 $x$ 或 $y$。某一側只有在收到比目前值更大的數時才會得分，所以那一側所有得分的元素會形成嚴格遞增子序列。問題可以看成：從 $P$ 中選出兩條不相交的遞增子序列，最大化長度總和。

若 $P_i$ 刷新了全域前綴最大值，把它接在目前較大的一側一定能得 1 分，同時保留另一側較小的值。這些前綴最大值都可以固定計分；把它們從 $P$ 移除後，剩下能取得的最佳分數就是序列的 LIS 長度。

掃過排列時，前綴最大值只累加 `add`，其他元素則丟進值域 BIT 做 LIS：`update(x, query(x-1)+1)`。答案是前綴最大值個數加上 BIT 中的最大值，總複雜度 $O(N\log N)$。

```cpp
int mx = 0, add = 0;                  // mx = 目前全域最大;add = 前綴最大值個數
for (int i = 0; i < n; i++) {
    int x; cin >> x;
    if (x > mx) {                     // 前綴最大值固定計分
        add++;
        mx = x;
    } else {
        update(x, query(x - 1) + 1);  // 其餘元素做 LIS
    }
}
cout << add + query(n) << '\n';
```
:::

### [G. Prefix-Complete Permutations](https://atcoder.jp/contests/abc468/tasks/abc468_g)
`組合計數` `dp` `AtC~2000`

::: info 題意
給定 $N \le 2000$ 與長度 $N$ 的字串 $S$(字元為 `o`/`x`)。

**求**有幾個 $(1,\dots,N)$ 的排列 $P$ 滿足:對每個 $k$,「$S_k=$ `o`」⟺「$P$ 中存在一段**連續**子序列恰好是 $(1,\dots,k)$ 的某個排列」。答案對 $998244353$ 取模。

例:$N=3$、$S=$ `oxo` → 只有 $P=(2,3,1)$ 與 $(1,3,2)$ 合法(值 $1$ 單獨成段所以 $k=1$ 必 `o`;$k=2$ 要求 $1,2$ **不**相鄰;$k=3$ 全體必 `o`)→ 答案 2。
:::

::: tip 作法
先處理邊界：$\{1\}$ 一定連續，整個排列也一定連續，所以必須有 **$S_1=S_N=$ `o`**，否則答案是 0。

稱 $k$ **完備**，表示 $1..k$ 在排列中占據一段連續區間。若 $k<k'$ 都完備，$1..k$ 的區間必定包含在 $1..k'$ 的區間內；所有 `o` 因而形成一層層向外擴張的邊界。相鄰兩個 `o` 之間可以分開計數，最後把各段方案數相乘。

下面以 $S=$ `oxxo`、$P=(2,4,1,3)$ 為例。只看值 $\le k$ 的位置：$k=1$ 時只有一格，$k=2,3$ 都被較大的值隔開，直到 $k=4$ 才重新連成整段。

<svg viewBox="0 0 360 224" width="100%" style="max-width:360px;height:auto;display:block" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="排列 2 4 1 3 對應 o x x o：值不超過 k 的位置在 k 等於 1 和 4 時連續">
  <text x="6" y="32" font-size="13" fill="currentColor">P</text>
  <g font-size="14" text-anchor="middle" fill="currentColor">
    <rect x="78" y="10" width="40" height="36" rx="6" fill="var(--vp-c-default-soft)"/><text x="98" y="33">2</text>
    <rect x="124" y="10" width="40" height="36" rx="6" fill="var(--vp-c-default-soft)"/><text x="144" y="33">4</text>
    <rect x="170" y="10" width="40" height="36" rx="6" fill="var(--vp-c-default-soft)"/><text x="190" y="33">1</text>
    <rect x="216" y="10" width="40" height="36" rx="6" fill="var(--vp-c-default-soft)"/><text x="236" y="33">3</text>
  </g>
  <g font-size="13" fill="currentColor">
    <text x="6" y="78">k=1 · o</text>
    <rect x="170" y="58" width="40" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <text x="274" y="77">連續</text>
    <text x="6" y="118">k=2 · x</text>
    <rect x="78" y="98" width="40" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <rect x="170" y="98" width="40" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <line x1="118" y1="112" x2="170" y2="112" stroke="currentColor" stroke-dasharray="4 4" opacity="0.5"/>
    <text x="274" y="117">被 4 隔開</text>
    <text x="6" y="158">k=3 · x</text>
    <rect x="78" y="138" width="40" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <rect x="170" y="138" width="86" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <line x1="118" y1="152" x2="170" y2="152" stroke="currentColor" stroke-dasharray="4 4" opacity="0.5"/>
    <text x="274" y="157">仍被 4 隔開</text>
    <text x="6" y="198">k=4 · o</text>
    <rect x="78" y="178" width="178" height="28" rx="5" fill="var(--vp-c-brand-soft)" stroke="var(--vp-c-brand-1)"/>
    <text x="274" y="197">整段連續</text>
  </g>
</svg>

考慮一段相鄰的 `o`，距離設為 $d$：前一個完備位置是 $k$，下一個是 $k+d$。這 $d$ 個新元素必須到最後才讓區間重新完備。令方案數為 $\mathrm{dp}[d]$，用補集計數：

- 不限制中途狀態時，把原本的完備區塊視為一個 token，連同 $d$ 個新元素排列，共有 $(d+1)!$ 種；
- 若第一次提早完備發生在第 $t$ 步，前段有 $\mathrm{dp}[t]$ 種，之後把它縮成一個 token，剩餘部分有 $(d-t+1)!$ 種。

$$\mathrm{dp}[d]=(d+1)!-\sum_{t=1}^{d-1}\mathrm{dp}[t]\cdot(d-t+1)!$$

用 $O(N^2)$ 預先算出所有 $\mathrm{dp}$；$N\le2000$ 可以通過。小值為 $\mathrm{dp}[1..5]=2,2,8,44,296$，其中 `oxxo` 對應 $d=3$，暴力枚舉也得到 8。

```cpp
if (s[0] != 'o' || s[n-1] != 'o') { cout << 0; return; }   // 邊界必為 o
for (int d = 1; d <= n; d++) {                              // 補集遞推
    ll v = fac[d + 1];
    for (int t = 1; t < d; t++) v -= fac[d - t + 1] * dp[t] % mod;
    dp[d] = (v % mod + mod) % mod;
}
ll ans = 1; int last = 1;
for (int k = 2; k <= n; k++)                                // 相鄰 o 的間距相乘
    if (s[k-1] == 'o') { ans = ans * dp[k - last] % mod; last = k; }
```
:::
