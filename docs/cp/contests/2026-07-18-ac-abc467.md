---
contest: AtCoder Beginner Contest 467
date: 2026-07-18
tags: [數學, 貪心, 線段樹, BIT, 資料結構]
url: https://atcoder.jp/contests/abc467
editorial: https://atcoder.jp/contests/abc467/editorial
source: self
verified: true
aside: false
reviewed: 2026-07-19
review_interval: 14
---

# AtCoder Beginner Contest 467 — 學習重點

> 親自參賽,作法為自己推導、code 皆驗過樣例。難度 `AtC~` 為 kenkoooo difficulty。
> 前段熱身略過;一個坑:D 中垂線交點判定,座標 $10^9$ 下叉積/常數項要 `__int128`。

### [E. Adjacent Sums (hard)](https://atcoder.jp/contests/abc467/tasks/abc467_e)
`數學` `構造` `AtC~1700`

::: info 題意
給定序列 $A$(長 $N$)與 $B$(長 $N-1$),元素皆在 $0..M-1$($2\le N\le 2\times10^5$、$3\le M\le 10^9$)。可對任意 $A_i$ **+1 任意次**。

**求/輸出**:使每個相鄰對滿足 $(A_i+A_{i+1})\bmod M=B_i$ 的最少 +1 總次數。(C 題是 $M=2$ 的簡化版。)

例:$M=5$、$A=[0,0]$、$B=[3]$ → 給 $A_2$ 加 $3$ 次($(0+3)\bmod 5=3$),且 $c_1+c_2\equiv 3$ 下 $c_1+c_2$ 最小就是 $3$ → 答案 $3$。
:::

::: tip 作法
設位置 $i$ 加 $c_i$ 次,$d_i=(B_i-A_i-A_{i+1})\bmod M$,條件 $(c_i+c_{i+1})\equiv d_i$ → **固定 $c_1=x$ 整條鏈鎖死**(每位只加餘數,加整圈 $M$ 純浪費);樸素試每個 $x\in[0,M)$ 是 $O(NM)$,$M\le 10^9$ 會 TLE。

**寶石:$\mathrm{cost}(x)$ 是鋸齒和——固定斜率(偶位數 − 奇位數 $\in\{0,1\}$)+ 若干「繞圈點」的 $\pm M$ 跳;斜率 $\ge 0$ → 谷底只可能在 $x=0$ 或某繞圈點**。收集繞圈點排序後掃過去,$O(N\log N)$。(不是碗形,**不能三分**。)

**踩過的坑:同一個 $x$ 上多個繞圈事件(有掉有升)要全部加完再取 $\min$**,只跳一半會抓到假值(sample 3 就是這樣錯的)。

```cpp
// c[0]=0 連鎖出 c[i];cost(x):偶位 +x、奇位 −x(mod M)
ll cost0 = 0; for (int i = 0; i < n; i++) cost0 += c[i];
ll slope = (n + 1) / 2 - n / 2;                    // 偶位數 − 奇位數 ∈ {0,1}
vector<pair<ll,ll>> ev;                            // {繞圈點 x, ±M}
for (int i = 0; i < n; i++)
    if (i % 2 == 0) { if (c[i] >= 1)     ev.push_back({M - c[i], -M}); }
    else            { if (c[i] <= M - 2) ev.push_back({c[i] + 1,  M}); }
sort(ev.begin(), ev.end());
ll best = cost0, acc = 0;
for (size_t i = 0; i < ev.size();) {
    ll pos = ev[i].first;
    while (i < ev.size() && ev[i].first == pos) acc += ev[i++].second;  // ★同位置全加完
    best = min(best, cost0 + slope * pos + acc);
}
```
:::

### [F. Email Scheduling Optimization](https://atcoder.jp/contests/abc467/tasks/abc467_f)
`貪心` `線段樹` `AtC~2050`

::: info 題意
給定正整數序列 $A,B$(各長 $N$)。寫 $N$ 封信,一次只能寫一封、順序自選:寫第 $j$ 封花 $A_j$ 分鐘,寫完瞬間寄出、$B_j$ 分鐘後收到回信。

$Q$ 次查詢:`1 i x`(改 $A_i=x$)或 `2 i x`(改 $B_i=x$),每次改完**輸出**最佳順序下「收齊所有回信」的最早時間($N,Q\le 10^5$、$A_j,B_j\le 10^9$)。

例:$A=[3,1]$、$B=[1,100]$ → 先寫 $2$:完成 $1$、回信 $101$;再寫 $1$:完成 $4$、回信 $5$ → 答案 $101$(反過來是 $104$)。
:::

::: tip 作法
**按 $B$ 由大到小寫**(交換論證,與 $A$ 無關)→ 化成跟順序無關的式子:**答案 $=\max_j\bigl(B_j+\sum_{B_k\ge B_j}A_k\bigr)$**。

擺上 $B$ 數線(離線壓縮)用線段樹維護:節點存 `total`($A$ 和)與 `best`;合併(左=B 小、右=B 大):`best = max(R.best, L.best + R.total)`——左邊的後綴要加上右邊全部 $A$。改 $A_i$ = 在 $B_i$ 格加差值;改 $B_i$ = 把 $A_i$ 搬格(兩次點更新)。$O((N+Q)\log)$。

```cpp
void pull(int k) { total[k] = total[2*k] + total[2*k+1];
                   best[k]  = max(best[2*k+1], best[2*k] + total[2*k+1]); }
void add(int pos, ll d) {              // 在 B 值 = val[pos] 的格子加 d 的 A 量
    int k = sz + pos; total[k] += d;
    best[k] = (total[k] > 0) ? val[pos] + total[k] : NEG;
    for (k >>= 1; k >= 1; k >>= 1) pull(k);
}
// 答案 = best[1]
```
:::

### [G. Many Sweets Problem](https://atcoder.jp/contests/abc467/tasks/abc467_g)
`線段樹` `BIT` `資料結構` `AtC~2550`

::: info 題意
給定正整數陣列 $A$(長 $N$)。$Q$ 次查詢 `c x l r k`:先改 $A_c=x$,再回答——區間 $[l,r]$ 的糖果(甜度 $=A$ 值)中,吃到總甜度 $\ge k$ 的**最少顆數**;湊不到輸出 $-1$($N,Q\le 10^5$、$A_i\le 10^9$、$k\le 10^{15}$)。

例:$A=[5,3,8]$、$[l,r]=[1,3]$、$k=12$ → 由甜到淡吃 $8+5=13\ge 12$ → $2$ 顆。
:::

::: tip 作法
挑最甜的吃 → 要「值由大到小的前綴和第一次 $\ge k$」。帶修改 → **翻轉版 merge-sort tree:軸 = 值域的線段樹,每個 value-node 內用「索引軸 Fenwick」存 count/sum,離線把每節點可能出現的索引鋪好固定**(改值 = 沿新舊值兩條葉→根路徑各做 ±1/±v,不重排)。

**寶石:用 kth-element 式 descent 取代「外層對門檻二分」——從根往高值半走一趟:夠 $k$ 就進去、不夠就全吃往低值,葉子再 $\lceil\text{剩}/v\rceil$**;省掉一個 log,$O(\log^2 N)$/query(TLE 版 = merge-sort tree 當黑盒外層二分,$O(\log^3)$)。

釐清過的關卡:**$[l,r]$ 不是樹上節點**——每個 value-node 用自己的索引 Fenwick `prefix(r)−prefix(l−1)` 直接取,不需 node 對齊;**descent 是一條 root→leaf 的路**(每層只進一個 child,不 DFS);想真懂先寫純版「靜態區間第 $k$ 小」,G 只是它 + Fenwick 改值 + 存 sum。

```cpp
auto tot = qnode(1, l, r);                       // (count, sum)
if (tot.second < k) { print(-1); continue; }
int node = 1; ll need = k, ans = 0;
while (node < SZ) {
    auto rq = qnode(2*node + 1, l, r);           // 右子 = 高值半
    if (rq.second >= need) node = 2*node + 1;    // 夠 → 進高值半
    else { ans += rq.first; need -= rq.second; node = 2*node; }  // 全吃 → 往低值
}
ans += (need + vals[node - SZ] - 1) / vals[node - SZ];   // 葉:ceil(剩/v)
```
:::
