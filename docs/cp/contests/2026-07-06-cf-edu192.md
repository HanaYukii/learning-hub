---
contest: Educational Codeforces Round 192
date: 2026-07-06
tags: [dp, 貪心, 位元, 資料結構, 不變量]
url: https://codeforces.com/contest/2242
editorial: https://codeforces.com/blog/entry/155047
source: hints+code
verified: false
aside: false
reviewed: 2026-07-06
review_interval: 14
---

# Educational Codeforces Round 192 — 學習重點

> 懶人包:每題「題意 + 核心作法」。難度 `~` 為 CF-equivalent。
> 版面:題意含小例、作法含關鍵片段。

### [B. Predominant Frequency Division](https://codeforces.com/contest/2242/problem/B)
`貪心` `分類討論` `~1200`

::: info 題意
給定長度 n 的陣列 a,每個元素都是 1、2 或 3(Σn ≤ 2e5)。

問能否把 a 切成三段連續非空子陣列,使第 i 段(i=1..3)中「值 > i 的元素個數」不超過該段長度的一半;等價地:第 1 段中 1 的個數 ≥ 2、3 的總數,第 2 段中 1、2 的總數 ≥ 3 的個數,第 3 段恆合法、只需非空。

輸出:可行則 YES,否則 NO。

例:a = [1, 2, 3] 切 [1] | [2] | [3]:段 1 有一個 1 ≥ 零個 2、3,段 2 的 1、2 共一個 ≥ 零個 3,段 3 非空 → YES;a = [3, 3, 3] 唯一切法是 [3] | [3] | [3],段 1 中值 > 1 的個數 1 超過長度一半 0.5 → NO。
:::

::: tip 作法
**第 3 段無值 >3、永遠合法**,前兩段條件各是一個 ±1 前綴和 ≥ 0(1 段:1 記 +1 其餘 −1;2 段:3 記 −1 其餘 +1);掃第 2 段結尾 i < n,對已見的合法第 1 段結尾 j 維護 pref2[j] 最小值,pref2[i] ≥ min 即 YES。O(n)。

```cpp
// sketch:單迴圈掃第 2 段結尾 i(i < n,保留第 3 段非空)
int p1 = 0, p2 = 0, mn = INT_MAX;
bool ok = false;
for (int i = 1; i <= n - 1; ++i) {
    p2 += (a[i] == 3 ? -1 : +1);      // pref2:3 記 −1、其餘 +1
    if (p2 >= mn) ok = true;          // 存在合法第 1 段結尾 j < i
    p1 += (a[i] == 1 ? +1 : -1);      // pref1:1 記 +1、其餘 −1
    if (p1 >= 0) mn = min(mn, p2);    // j = i 之後可作第 1 段結尾
}
```
:::

### [C. Unstable Elements](https://codeforces.com/contest/2242/problem/C)
`數學` `~1400`

::: info 題意
給定已排序的整數陣列 a(長度 n)與目標長度 k(n, k ≤ 3e5)。

每次操作先標記每個等值連續段的首元素(即 a_1 與所有 a_i ≠ a_{i−1} 的位置),再二選一:把標記元素全部刪除,或把每個標記元素換成兩個相同元素;不允許把陣列操作到空。

輸出:經任意次操作可到達、長度恰為 k 的相異陣列個數。

例:a = [1, 2, 2]、k = 2 → 標記位置 1、2:刪除得 [2],再對 [2] 做「換成兩個」得 [2, 2],長度 2 ✓;可驗證長度 2 的可達陣列僅 [2, 2] 一個,答案 1。
:::

::: tip 作法
壓成 block 長度後,**每次操作 = 所有存活 block 長度同時 ±1,歸零者永久消失**,故可達陣列由「最短存活 block 的原長門檻」唯一決定。

由大到小枚舉門檻 i(需 c[i] > 0),維護存活數 s1 與最小總長 s2(s1 += c[i]、s2 += s1);k ≥ s2 且 (k−s2) % s1 == 0 則答案 +1。O(n)。

```cpp
// sketch:c[i] = 原長恰為 i 的 block 數,由大到小枚舉門檻
long long s1 = 0, s2 = 0, ans = 0;   // 存活 block 數 / 最小可達總長
for (int i = n; i >= 1; --i) {
    s1 += c[i];
    s2 += s1;                         // 門檻 −1:每個存活 block 最小長 +1
    if (c[i] && k >= s2 && (k - s2) % s1 == 0) ans++;
}
```
:::

### [D. Two Digit Strings](https://codeforces.com/contest/2242/problem/D)
`dp` `不變量` `字串` `~1700`

::: info 題意
給定兩個數字字串 a、b(Σ(|a|+|b|) ≤ 1e4)。

每次操作可在任一串中選相鄰兩個字元,替換成兩者之和 mod 10,該串長度 −1;兩串各可操作任意次(含零次)。

輸出:兩串能變得完全相等(長度與逐位皆同)時的最大長度;不可能則輸出 −1。

例:a = "12"、b = "21" → digit sum 皆 3;維持長度 2 就不能操作,"12" ≠ "21";各做一次操作得 "3" = "3" → 答案 1。
:::

::: tip 作法
**digit sum mod 10 是不變量**(總和不同直接 −1);變成長 k 相等 ⟺ 兩串各切 k 段、對應段和 mod 10 相等 ⟺ 找兩串前綴和 (mod 10) 陣列的共同切點 → 直接做 LCS,答案 = LCS − 1。

看似子段 DP 的題壓成 O(nm)。

```cpp
// sketch:切點值 = 前綴和 mod 10(含空前綴的 0),對兩陣列做經典 LCS
vector<int> pa{0}, pb{0};
for (char c : a) pa.push_back((pa.back() + c - '0') % 10);
for (char c : b) pb.push_back((pb.back() + c - '0') % 10);
if (pa.back() != pb.back()) return -1;             // 不變量不同
for (int i = 1; i <= (int)pa.size(); ++i)
    for (int j = 1; j <= (int)pb.size(); ++j)
        dp[i][j] = pa[i-1] == pb[j-1] ? dp[i-1][j-1] + 1
                                      : max(dp[i-1][j], dp[i][j-1]);
return dp[pa.size()][pb.size()] - 1;               // 共同切點數 − 1 = 段數
```
:::

### [E. Product of Closures](https://codeforces.com/contest/2242/problem/E)
`位元` `構造` `貪心` `~2400`

::: info 題意
定義 C(x) 為把正整數 x 的二進位表示(無前導零)無限重複串接成的無限 01 串,兩串的乘積 C(x) AND C(y) 為逐位 AND。

給定整數 l、r、n(l < r < 2^30,n ≤ 1000)。求一對 x、y 滿足 l ≤ x < y ≤ r,使 C(x) AND C(y) 字典序最小。

輸出:該乘積的前 n 個字元。

例:l=2, r=4, n=4 → C(2)=1010…、C(3)=1111…、C(4)=100100…;三對的 AND 前 4 字元:(2,3)→1010、(2,4)→1000、(3,4)→1001,字典序最小為 1000,取 x=2、y=4。
:::

::: tip 作法
**支配觀察:把 x 的最低位 1 清掉後若仍 ≥ l,新數每一位都 ≤ 原數,原 x 可捨** → 候選只剩「l 的前綴 + 某個 0 位改 1 + 低位全清 0」的 O(log r) 個;x、y 各自枚舉(約 30×29 對),每對 O(n) 求 AND 取最小。

官解是 O(1) 分類構造;此法從暴力直接優化可得,省去繁瑣 casework。

```cpp
// sketch:候選生成 —— l 本身 + 「l 的前綴 + 某 0 位改 1 + 低位清 0」
vector<int> cand{l};
for (int b = 0; b < 30; ++b)
    if (!(l >> b & 1)) {
        int v = ((l >> b) | 1) << b;   // 高位照抄、第 b 位設 1、低位清 0
        if (v <= r) cand.push_back(v);
    }
// x、y 於 cand 中各自枚舉(x < y),每對 O(n) 生成 AND 前綴取字典序最小
```
:::

### [F. Summer Vacation](https://codeforces.com/contest/2242/problem/F)
`資料結構` `根號` `~2500`

::: info 題意
給定 n 天(n ≤ 1e5)與每天的空閒分鐘數 a_i(a_i ≤ n)。

從 0 元開始依序過日子:某天開始時若持有錢 ≥ a_i,則整天消費、錢 −a_i;否則整天打工、錢 +a_i(每天只能整天擇一)。

輸出:對每個 k = 1..n,若只從第 n−k+1 天起(以 0 元)過最後 k 天,最後一天結束後剩多少錢——共 n 個答案。

例:a = [2, 1, 2] → k=1:0 < 2,打工得 2;k=2:兩天皆打工,0 → 1 → 3;k=3:0 → 2(打工)→ 1(2 ≥ 1,消費)→ 3(打工)。輸出 2 3 3。
:::

::: tip 作法
**倒序加天數,把後綴的複合轉移函數存成長 2n 的序列——位置 v 存「初始錢 v 的最終錢」(錢恆 < 2n)**;疊上一天 = 序列 cut-paste(新序列 = 舊 [a_i, 2a_i) 接 [0, 2n−a_i)),答案 = 首元素。

<svg viewBox="0 0 560 204" width="560" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="cut-paste 示意:疊上一天後,新序列 = 舊 [a,2a) 接 舊 [0,2n−a)">
  <text x="40" y="22" font-size="12" fill="currentColor">舊序列(後綴 i+1 的轉移表,長 2n)</text>
  <rect x="40" y="30" width="480" height="26" fill="var(--vp-c-default-soft)"/>
  <rect x="184" y="30" width="144" height="26" fill="var(--vp-c-brand-1)" opacity="0.5"/>
  <rect x="40" y="30" width="336" height="26" fill="none" stroke="var(--vp-c-brand-1)" stroke-width="2" stroke-dasharray="5 4"/>
  <text x="40" y="72" font-size="12" fill="currentColor">0</text>
  <text x="184" y="72" font-size="12" fill="currentColor" text-anchor="middle">a</text>
  <text x="328" y="72" font-size="12" fill="currentColor" text-anchor="middle">2a</text>
  <text x="376" y="72" font-size="12" fill="currentColor" text-anchor="middle">2n−a</text>
  <text x="520" y="72" font-size="12" fill="currentColor" text-anchor="end">2n</text>
  <text x="40" y="106" font-size="12" fill="currentColor">新序列(疊上空閒 a 的一天,長度仍 2n)</text>
  <rect x="40" y="114" width="144" height="26" fill="var(--vp-c-brand-1)" opacity="0.5"/>
  <rect x="184" y="114" width="336" height="26" fill="var(--vp-c-default-soft)" stroke="var(--vp-c-brand-1)" stroke-width="2" stroke-dasharray="5 4"/>
  <text x="112" y="132" font-size="12" fill="currentColor" text-anchor="middle">舊 [a, 2a)</text>
  <text x="352" y="132" font-size="12" fill="currentColor" text-anchor="middle">舊 [0, 2n−a)</text>
  <text x="40" y="158" font-size="12" fill="currentColor">0</text>
  <text x="184" y="158" font-size="12" fill="currentColor" text-anchor="middle">a</text>
  <text x="520" y="158" font-size="12" fill="currentColor" text-anchor="end">2n</text>
  <text x="40" y="186" font-size="12" fill="currentColor">v < a:打工 → 查舊 v+a;v ≥ a:消費 → 查舊 v−a</text>
</svg>

持久化 treap split/merge(週期重建控記憶體)或 sqrt 分塊;1.5s 時限下 O(n²) 自動向量化暴力也能過。

```cpp
// sketch:seq 初始為恆等 [0..2n),倒序疊天(range 取段需持久化)
for (int i = n; i >= 1; --i) {
    int a = ai[i];
    Node *mid = range(seq, a, 2*a);      // 舊 [a, 2a):v < a 打工 → 查 v+a
    Node *low = range(seq, 0, 2*n - a);  // 舊 [0, 2n−a):v ≥ a 消費 → 查 v−a
    seq = merge(mid, low);               // cut-paste,長度仍 2n
    ans[n - i + 1] = kth(seq, 0);        // 初始錢 0 → 最後 k = n−i+1 天的答案
}
```
:::
