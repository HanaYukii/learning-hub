---
contest: AtCoder Regular Contest 223
date: 2026-06-28
tags: [數論, 機率期望, 反射原理, NTT]
url: https://atcoder.jp/contests/arc223
editorial: https://atcoder.jp/contests/arc223/editorial
source: editorial
verified: false
aside: false
reviewed: 2026-07-01
review_interval: 14
---

# AtCoder Regular Contest 223 — 學習重點

> 懶人包:每題「題意 + 核心作法」。難度 `AtC~` 為 kenkoooo difficulty,據官方 editorial 整理。
> 版面:題意含小例、作法含關鍵片段。

### [A. Unusual-Constraint Knapsack](https://atcoder.jp/contests/arc223/tasks/arc223_a)
`貪心` `背包` `AtC~1000`

::: info 題意
給定 $N$ 件物品(第 $i$ 件有重量 $w_i$、價值 $v_i$)與背包容量 $W$,保證重量超遞增:對每個 $i\ge2$ 有 $\sum_{j<i} w_j < w_i$。

從中選取一個子集,總重量不得超過 $W$。輸出:可達的最大總價值。

範圍:$N\le60$、$w_i\le W\le10^{18}$、$v_i\le10^{16}$,多測 $\sum N\le2\times10^5$。

例:$w=(1,2,4)$、$v=(3,1,3)$、$W=5$ → 取第 $1$、$3$ 件(總重 $5\le W$)得 $3+3=6$;窮舉全部 $8$ 個子集無更優,答案 $6$。
:::

::: tip 作法
從 $i=N$ 往下決定取捨,維護剩餘容量 $R$。

**關鍵:若 $w_i\le R$ 而不取第 $i$ 件,則 $\sum_{j<i}w_j<w_i\le R$,前 $i-1$ 件可全取、搜尋立即終止**;取則以 $R-w_i$ 遞迴下去,兩分支取大,$O(N)$。

```cpp
// sketch:「不取」分支 O(1) 終止,只有「取」往下遞迴 → O(N)
long long solve(int i, long long R) {          // 前 i 件、剩餘容量 R
    if (i == 0) return 0;
    if (w[i] > R) return solve(i - 1, R);      // 放不下:只能跳過
    long long skip = pre_v[i - 1];             // 不取:∑w < w_i ≤ R,前 i-1 件全取
    long long take = v[i] + solve(i - 1, R - w[i]);
    return max(skip, take);                    // 兩分支取大
}
```
:::

### [B. Valid Arrays by K-Divisible Swaps](https://atcoder.jp/contests/arc223/tasks/arc223_b)
`組合計數` `AtC~1300`

::: info 題意
給定長度 $N$ 的整數序列 $A$($1\le A_i\le10^9$)與整數 $K\le10^9$。

每次操作可選相鄰兩項,若 $A_i+A_{i+1}$ 是 $K$ 的倍數則交換之;操作可執行任意次(含零次)。

輸出:從初始序列出發可達的不同序列總數,mod 998244353。範圍:$N\le2\times10^5$,多測 $\sum N\le2\times10^5$。

例:$K=3$、$A=(1,2,1)$ → 兩處相鄰和($1{+}2$、$2{+}1$)皆為 $3$ 的倍數可交換:$(1,2,1)\to(2,1,1)$ 或 $(1,1,2)$,再操作只會退回,共 $3$ 種。
:::

::: tip 作法
交換只發生在 $a+b\equiv0\pmod K$ 的相鄰對(元素 mod $K$ 只會變號),故在相鄰兩數「和、差皆非 $K$ 倍數」處切斷,各段方法數獨立相乘。

**段內若 $2a\equiv0$ 可自由排列;否則段內只含 $\{a,b\}$($a+b\equiv0$)兩類,同類間順序不變,方法數 = $a$ 類元素擺位的組合數**。
:::

### [C. Whole Product of Pairwise Distances](https://atcoder.jp/contests/arc223/tasks/arc223_c)
`數論` `逆序對` `AtC~1700`

::: info 題意
給定 $N$ 個正整數 $A_1,\dots,A_N$($1\le A_i\le10^9$)。

輸出:全體點對絕對差之積 $\prod_{1\le i<j\le N}|A_i-A_j|$ 除以 $N$ 的餘數——模數是序列長度 $N$ 本身而非質數。

範圍:$2\le N\le2\times10^5$,多測 $\sum N\le2\times10^5$。

例:$N=3$、$A=(1,5,3)$ → $|1{-}5|\cdot|1{-}3|\cdot|5{-}3|=4\cdot2\cdot2=16$,除以 $3$ 餘 $1$,輸出 $1$。
:::

::: tip 作法
排序後令 $B_i=A_i\bmod N$;$B$ 有重複則答案 $0$,否則 $B$ 是 $\{0,\dots,N-1\}$ 的排列。

**關鍵:$\prod(B_j-B_i)=(-1)^I\prod(j-i)$($I$ 為逆序數),而 $\prod_{i<j}(j-i)=\prod_{d=1}^{N-1} d^{\,N-d}$ 可直接算**。

```cpp
// sketch:排序 → mod N → 檢重複 → 逆序數 + ∏ d^{N-d}
sort(a.begin(), a.end());
for (auto &x : a) x %= n;                    // B_i = A_i mod N(排序後)
if (has_duplicate(a)) { print(0); return; }  // B 非排列 → 積 ≡ 0
long long I = count_inversions(a);           // BIT / 歸併,O(N log N)
long long ans = 1;
for (long long d = 1; d < n; ++d)
    ans = ans * pow_mod(d, n - d, n) % n;    // ∏_{i<j}(j-i) = ∏ d^{N-d}
if (I & 1) ans = (n - ans) % n;              // 乘上 (-1)^I
```
:::

### [D. Xpectation of Cards in Hand with Laboratory](https://atcoder.jp/contests/arc223/tasks/arc223_d)
`機率期望` `組合計數` `反射原理` `AtC~2300`

::: info 題意
給定整數 $A,B,K$:$A$ 張 draw 卡與 $B$ 張普通卡均勻隨機洗成一疊牌堆,先從堆頂抽 $K$ 張入手。

接著只要手中還有 draw 卡就重複:棄掉一張 draw 卡,並從堆頂補 $\min(c,2)$ 張($c$ 為牌堆剩餘張數),直到手中沒有 draw 卡為止。

輸出:結束時手牌張數的期望值,mod 998244353(有理數取模)。範圍:$1\le K\le A+B\le10^7$,多測 $\sum(A+B)\le10^7$。

例:$A=1,B=2,K=1$ → 牌堆 $3$ 種排法各 $1/3$:draw 卡在頂時首抽得之,棄掉再補 $2$ 張普通卡,終手 $2$ 張;draw 卡在第 $2$、$3$ 位時首抽是普通卡、直接停,終手 $1$ 張。期望 $=\frac{2+1+1}{3}=\frac43$。
:::

::: tip 作法
把 draw/普通卡映成格路右/上步,終止狀態僅 $O(A+B)$ 個。

<svg viewBox="0 0 480 320" width="480" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="格路示意:右步為抽出 draw 卡、上步為抽出普通卡,首次碰到 b = a + K 即 draw 卡用罄">
  <polygon points="80,172 168,84 80,84" fill="var(--vp-c-default-soft)"/>
  <g stroke="currentColor" stroke-width="1" opacity="0.15">
    <path d="M 80 84 V 260 M 124 84 V 260 M 168 84 V 260 M 212 84 V 260 M 256 84 V 260 M 80 84 H 256 M 80 128 H 256 M 80 172 H 256 M 80 216 H 256 M 80 260 H 256"/>
  </g>
  <line x1="80" y1="172" x2="168" y2="84" stroke="var(--vp-c-brand-1)" stroke-width="2" stroke-dasharray="4 4"/>
  <text x="176" y="92" font-size="12" fill="currentColor">b = a + K</text>
  <polyline points="80,260 80,216 124,216 124,172 124,128" fill="none" stroke="var(--vp-c-brand-1)" stroke-width="2.5"/>
  <circle cx="80" cy="260" r="4" fill="currentColor" opacity="0.5"/>
  <circle cx="124" cy="128" r="5" fill="var(--vp-c-brand-1)"/>
  <text x="134" y="150" font-size="12" fill="currentColor">首次碰線 → 一個終止狀態</text>
  <text x="264" y="264" font-size="12" fill="currentColor">a:抽出 draw 卡數</text>
  <text x="80" y="72" font-size="12" fill="currentColor">b:抽出普通卡數</text>
  <text x="240" y="296" text-anchor="middle" font-size="12" fill="currentColor">K = 2:首次碰到 b = a + K ⇔ draw 卡用罄;從未碰線 ⇔ 抽到牌堆見底</text>
</svg>

**反射原理計數:「首次 $b=a+K$(draw 卡用罄)」的排法 $\big(\binom{a+b-1}{a}-\binom{a+b-1}{a-1}\big)\binom{A-a+B-b}{A-a}$;「始終 $b<a+K$(抽到牌堆見底)」共 $\binom{A+B}{A}-\binom{A+B}{A+K}$**;各乘最終手牌數求和,$O(A+B)$。
:::

### [E. Yin-Yang Two Bits Insertion](https://atcoder.jp/contests/arc223/tasks/arc223_e)
`差分` `不變量` `dp` `AtC~2550`

::: info 題意
給定 0/1 序列 $A$(長 $N$)與 $B$(長 $M$)。

每次操作選位置 $i$,在 $A_i$ 與 $A_{i+1}$ 之間依序插入 $1-A_i$、$1-A_{i+1}$ 兩個元素(如 $(0,1,1)$ 取 $i=2$ 變 $(0,1,0,0,1)$),可執行任意次。

輸出:能否把 $A$ 變成 $B$,能則 `Yes` 否則 `No`。範圍:$2\le N\le M\le2\times10^5$,多測 $\sum(N+M)\le4\times10^5$。

例:$A=(0,1)$ → 唯一的一步 $i=1$ 得 $(0,1,0,1)$,故 $B=(0,1,0,1)$ 輸出 `Yes`;而 $B=(0,1,1,1)$ 長度同為 $4$ 卻到不了(差分中 $0$ 的個數 $2\ne0$),輸出 `No`。
:::

::: tip 作法
取差分 $A'_i=[A_i\ne A_{i+1}]$,操作變成 $0\to101$、$1\to111$;原串首尾字元與 $A'$ 中 $0$ 的個數為不變量。

**再以 $0$ 分段、記各段 $1$ 的個數,操作化為「相鄰兩項各 $+1$」與「$\ge1$ 的項 $+2$」,且同一對做前者 $\ge3$ 次可用後者替換掉兩次,故每對至多 $2$ 次**;左到右 dp(鍵為前一對的操作次數)判可達,$O(|A|+|B|)$。
:::

### [F. Zonal Score Maximization](https://atcoder.jp/contests/arc223/tasks/arc223_f)
`組合計數` `NTT` `矩陣` `AtC~3150`

::: info 題意
對 $(1,\dots,N)$ 的排列 $P$,定義 $f(P)$ 為:把 $P$ 切成若干段長 $\ge2$ 的連續段,各段 $(\max+\min)$ 總和在所有合法切法中的最大值。

給定序列 $Q$(每項為 $-1$ 或 $1\sim N$ 中的整數,非 $-1$ 者互異)與目標值 $X\le10^{18}$。

輸出:滿足「$Q_i\ne-1$ 處 $P_i=Q_i$」且 $f(P)=X$ 的排列 $P$ 個數,mod 998244353。範圍:$N\le10^5$,多測 $\sum N\le10^5$。

例:$N=3$、$Q=(-1,-1,-1)$ → 長 $3$ 只能整段不切,$f(P)=3+1=4$ 對全部 $3!=6$ 個排列同值;故 $X=4$ 時答案 $6$,其餘 $X$ 答案 $0$。
:::

::: tip 作法
$N$ 偶時任何 $P$ 都有 $f=\frac{N(N+1)}{2}$,$X$ 等於它答案才非零(為 $c!$,$c$ 是 $-1$ 數)。

**奇數時 $f(P)=\frac{N(N+1)}{2}-\min_{i\,\text{奇}}\operatorname{median}(P_i,P_{i+1},P_{i+2})$**;令 $p=\frac{N(N+1)}{2}-X$,差分計數「$M\ge p$ 減 $M\ge p+1$」:以 $<p/\ge p$ 二值化為 $B$,條件成每三項窗口 $B_{2i-1}+B_{2i}+B_{2i+1}\ge2$,用 $2\times2$ 形式冪級數轉移矩陣分治連乘 + 卷積取「$0$ 恰 $p-1$ 個」的係數,$O(N\log^2 N)$,再乘階乘轉回 $P$ 計數。
:::
